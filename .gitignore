# All real data (girls, photos, responses) now lives in Supabase - NOT in this
# repo - so there is no data here to protect. This repo only holds static code.

# macOS / Windows
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Logs / temp
*.log
.cache/
node_modules/
.tmp-*.json

# Local-only downloaded exports (keep these OUT of the repo)
responses-*.csv
