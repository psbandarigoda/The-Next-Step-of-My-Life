// -----------------------------------------------------------------------------
// Remote-save configuration.
// Paste your relay ("doorbell") URL between the quotes after you deploy it.
// Example: window.TNS.relayUrl = "https://tns-relay.yourname.workers.dev";
//
// Leave it empty ("") to disable remote saving. In that case a girl's answer
// is only kept in her own browser (you won't see it on another device).
// -----------------------------------------------------------------------------
window.TNS = window.TNS || {};
window.TNS.relayUrl = "";
